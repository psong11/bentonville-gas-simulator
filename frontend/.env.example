# Bentonville Gas Simulator - Frontend Environment Variables
# Copy this file to .env.local and fill in the values

# Backend API URL (Railway deployment)
# In development, leave empty to use Vite proxy (localhost:8000)
# In production, set to your Railway backend URL
VITE_API_URL=

# WebSocket URL (Railway deployment)
# In development, leave empty to auto-detect
# In production, set to your Railway backend WebSocket URL (wss://...)
VITE_WS_URL=
