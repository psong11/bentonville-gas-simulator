# Bentonville Gas Digital Twin — runtime dependencies
# (dev/test tools live in requirements-dev.txt)

# Core simulation
networkx>=3.0
numpy>=1.24.0

# FastAPI backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
websockets>=12.0

# Optional PostgreSQL persistence (USE_DATABASE=true)
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# ChatOps agent
anthropic>=0.116.0
python-dotenv>=1.0.0
